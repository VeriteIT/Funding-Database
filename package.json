{
  "name": "gtex-finance-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node generate-finance-json.mjs",
    "build": "node generate-finance-json.mjs source.xlsx || node generate-finance-json.mjs",
    "preview": "node -e \"require('http').createServer((q,s)=>require('fs').createReadStream('index.html').pipe(s)).listen(4199,()=>console.log('http://localhost:4199'))\""
  },
  "devDependencies": {
    "exceljs": "^4.4.0"
  }
}
